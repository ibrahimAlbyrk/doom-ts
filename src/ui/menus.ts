// Title / main menu / options rendering — STUB. The PLAYING/title/menu states draw
// simple placeholders today; the polished menus land here (skill select, settings,
// rebinding). Owned by the UI worker.
import type { GameContext } from '../core';

export function drawTitleScreen(_ctx: CanvasRenderingContext2D, _game: GameContext): void {
  throw new Error('NotImplemented: drawTitleScreen');
}

export function drawMainMenu(_ctx: CanvasRenderingContext2D, _game: GameContext): void {
  throw new Error('NotImplemented: drawMainMenu');
}
