# Keep the build context lean. NOTE: public/assets is intentionally NOT ignored — the client
# build needs the extracted assets in the context (run `npm run extract-assets` first).
node_modules
dist
.git
.eos
tools/extract-wad/.cache
*.log
.DS_Store
